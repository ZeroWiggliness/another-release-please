// legacy defaultBranch tests removed; --default-branch CLI flag is removed and no longer supported
import { test } from '@jest/globals';

test.skip('legacy defaultBranch tests skipped because --default-branch is removed', () => {
  // intentionally skipped
});

